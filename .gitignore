node_modules/
# Keep environment variables out of version control
.env
*.xml
uploads/
/generated/prisma
dist/
*.txt

# Docker env
.env.docker

# 🌟 Security: ห้าม commit ไฟล์ที่มี credential / recovery key
AMLO Recovery Keys.pdf
*.key
backups/

# OS / junk files
.DS_Store
**/.DS_Store

# Local test scripts (อ่าน credential จาก .env)
test-otp.js
